#include <iostream>

int main() {
    std::cout << "Unsatisfied with constant time, Jeff Dean created the "
                 "world's first O(1/n) algorithm."
              << std::endl;
    return 0;
}
